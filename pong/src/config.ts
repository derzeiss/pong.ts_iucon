// Game window dimensions
export const WIDTH = 700;
export const HEIGHT = 600;
export const X_CENTER = WIDTH / 2;
export const Y_CENTER = HEIGHT / 2;

// Keys
export const KEYS = {
  up: 'ArrowUp',
  down: 'ArrowDown',
};

// Color
export const COL_PRIMARY = 'white';
export const COL_SECONDARY = 'black';

// Bar settings
export const BAR_WIDTH = 20;
export const BAR_HEIGHT = 100;
export const BAR_SPEED = 5;

// Ball settings
// Acceleration is applied on-hit
export const BALL_SIZE = 20;
export const BALL_SPEED_X_MIN = 5;
export const BALL_SPEED_Y_MAX = 10;
export const BALL_ACC = 1.1;

// Points to win a game
export const POINTS_TO_WIN = 5;

// Style settings
export const DEFAULT_FONT = '700 36px monospace';